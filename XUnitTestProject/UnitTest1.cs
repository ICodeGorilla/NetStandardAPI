using Xunit;

namespace XUnitTestProject
{
    public class UnitTest1
    {
        [Fact]
        public void Test1()
        {
            //var result = Assert.IsType<Ok>(controller.generatePasswd(length));
            //var password = Assert.IsType<string>(result.Value);
        }
    }
}
